# Resend — resend.com/api-keys
RESEND_API_KEY=
# The single shared audience every era's email capture form writes to.
RESEND_AUDIENCE_ID=
# Where Contact & Booking submissions land. Defaults to mgntajona@gmail.com if unset.
CONTACT_TO_EMAIL=
# Must be a verified sending domain in Resend for production. Defaults to
# Resend's onboarding sender, which only delivers to your own account email.
CONTACT_FROM_EMAIL=
